/**:
  ros__parameters:
    # YOLO模型配置
    model_path: "yolov8n.pt"
    confidence_threshold: 0.5
    device: "cpu"  # "cpu" 或 "cuda"
    
    # 目标类别 (COCO dataset)
    # cup: 41, apple: 47, banana: 46
